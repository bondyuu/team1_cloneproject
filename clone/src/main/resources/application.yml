spring:
  h2:
    console:
      enabled: true
  servlet:
    multipart:
      maxFilesize: 10MB
      maxRequestSize: 10MB

  datasource:
    url: jdbc:h2:mem:clone
    driver-class-name: org.h2.Driver
    username: sa
    password:

#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://database-3.cyggh9yzs5m7.ap-northeast-2.rds.amazonaws.com:3306/database3
#    username: admin
#    password: asdf1234


  jpa:
    hibernate:
      format_sql: true
      show_sql: true
      ddl-auto: update


server:
  error:
    include-exception: false # Response? Exception ?? ??
    include-message: always # Response? Exception Message ?? ??
    include-stacktrace: on_param # Response? Stack Trace ?? ??
  servlet:
    encoding:
      charset: utf-8
      enabled: true
      force: true

# echo 'spring-boot-security-jwt-hanghae-assignment-spring-boot-security-jwt-secret-key' | base64


